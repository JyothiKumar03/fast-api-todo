# FastAPI Todo Application Dependencies

# Core FastAPI framework
fastapi>=0.104.0

# ASGI server for running FastAPI
uvicorn[standard]>=0.24.0

# Data validation and serialization
pydantic>=2.5.0

# Database ORM for FastAPI (not Flask!)
sqlalchemy>=2.0.0

# Python typing extensions (if needed for older Python versions)
typing-extensions>=4.8.0

# Environment variable loading
python-dotenv>=1.0.0

# PostgreSQL driver
psycopg2-binary>=2.9.0
